# Финальный React Магазин

Одностраничный магазин на React, TypeScript, MirageJS и TanStack Query.

## Запуск

```powershell
npm install
npm run dev
```

Или на Windows:

```powershell
.\start_store.bat
```

Реальный backend не нужен, потому что MirageJS мокирует API прямо в браузере.
